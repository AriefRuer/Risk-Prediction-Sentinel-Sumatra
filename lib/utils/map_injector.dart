// This is the stub file for Native platforms (Android/iOS).
// Because this file does NOT import 'dart:html', the Android compiler won't crash.

void injectWebMapsApiKeySafely(String apiKey) {
  // Do nothing. Android handles the API key via AndroidManifest.xml.
}
